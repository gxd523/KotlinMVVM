<resources>

    <style name="Splash" parent="android:Theme.Light.NoTitleBar.Fullscreen">
        <item name="android:windowBackground">@drawable/splash</item>
    </style>

    <style name="NoActionBar" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="ToolbarTheme" parent="Widget.AppCompat.PopupMenu.Overflow">
        <!--是否覆盖锚点-->
        <item name="overlapAnchor">false</item>
        <!--弹出层水平方向上的偏移，负值为距离右边的空隙-->
        <item name="android:dropDownHorizontalOffset">-10dp</item>
        <!-- 弹出层垂直方向上的偏移，负则会盖住Toolbar -->
        <item name="android:dropDownVerticalOffset">5dp</item>
        <item name="android:divider">#6666</item>
        <item name="android:dividerHeight">1dp</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:popupBackground">#48bbfd</item>
    </style>

    <style name="PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>